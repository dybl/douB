"""
config file for PyEdit source-code windows popped up by PyDemos here;
PyEit imports this via normal sys.path, not package relative, so each
client app can have it own configs;  no file or assignments = defaults;
"""
# initial font                      # family, size, style
font = ('consolas', 12, 'bold')       # e.g., style: 'normal italic'

# initial color                     # default=white, black
bg = '#fef8cd'                      # colorname or RGB hexstr
fg = 'black'                        # 'LightYellow', 'powder blue', '#690f96'
